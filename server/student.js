class Student{    
    constructor(studentId, name, email, hash) {
        if(studentId)
            this.studentId = studentId;

        this.name = name;
        this.surname = surname;
    }
}

module.exports = Student;